class Vertex {
	constructor(label, wasVisted) {
		this.label = label;
		this.wasVisted = wasVisted;
	}
}

export default Vertex

// *
// All vertices in an array and can be found by its index
// The real info be put in _edges.
// --------------------------------------------------------------------------------
